/** See the file "LICENSE" for the full license governing this code. */
/** Simplified representations of model elements that are
 * used in sequences, which are then compared to determine the
 * difference between two states of the database.
 * Classes in this package implement the Comparable interface,
 * so that the sequences can be sorted before they are compared. */
package au.org.ands.vocabs.registry.model.sequence;
