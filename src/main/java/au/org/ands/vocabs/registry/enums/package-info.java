/** See the file "LICENSE" for the full license governing this code. */
/** Enumerated types for registry entities and the registry schema;
 *  these classes are generated from the registry schema definition. */
package au.org.ands.vocabs.registry.enums;
